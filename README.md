# Python Formatted String

This project demonstrates f-string (formatted string literal) usage in Python as a cleaner alternative to string concatenation.

## What I Learned
- How to use f-strings with the f prefix
- How to embed variables directly inside strings using {}
- How f-strings are cleaner than string concatenation
- How Python string formatting differs from C and C#
